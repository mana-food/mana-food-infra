apiVersion: v1
kind: Service
metadata:
  name: mana-food-service
  namespace: default
  labels:
    app: mana-food-app
spec:
  selector:
    app: mana-food-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  type: LoadBalancer